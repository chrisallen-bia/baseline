{#
/**
 * @file
 * @author christopherj.allen@bia.gov
 * Default theme implementation to display a FAQ in USWDS Accordion style
 **************
 * Available variables:
 * - title: The title of this group of rows. May be empty.
 * - rows: A list of the view's row items.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's content.
 * - view: The view object.
 * - default_row_class: A flag indicating whether default classes should be
 *   used on rows.
 *
 * @see template_preprocess_views_view_unformatted()
 *
 * @ingroup themeable
 */
<div class="grid-container">
    <div class="usa-accordion">
        {{ rows }}
    </div>
</div>


<div class="usa-accordion">
    {% for row in rows %}
       {{ row.attributes.addClass(row_classes) }}
            {{ row.content }}
    {% endfor %}
</div>
 #}


    {% for row in rows %}
      {% if loop.first %}
            <h4 class="usa-accordion__heading">
                <button class="usa-accordion__button" aria-expanded="true" aria-controls="a{{ counter }}">
                {{ row.content['#row']._entity.title.value }}
                </button>
            </h4>
            <div id="a{{ loop.index }} " class="usa-accordion__content usa-prose">
                {{ row.content['#row']._entity.body.value }}
            </div>
        {% else %}
            <h4 class="usa-accordion__heading">
                <button class="usa-accordion__button" aria-expanded="false" aria-controls="a{{ counter }}">
                    {{ row.content['#row']._entity.title.value }}
                </button>
            </h4>
            <div id="a{{ loop.index }} " class="usa-accordion__content usa-prose">
                {{ row.content['#row']._entity.body.value }}
            </div>
        {% endif %}
    {% endfor %}
